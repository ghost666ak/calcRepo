# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Node / build artifacts
node_modules/
dist/
.vite/
.cache/
coverage/
playwright-report/
test-results/
.eslintcache
.tsbuildinfo
# Generated docs/ build output is committed for GitHub Pages branch-based
# deploys. The source roadmap lives under docs/roadmap/ alongside it;
# source files in docs/ should not be deleted or it will remove both.
# When `npm run build:pages` regenerates docs/, just commit the changes.

# Editor and OS metadata
.DS_Store
Thumbs.db
.idea/
.vscode/